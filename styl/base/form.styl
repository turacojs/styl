
placeholder(color #bdbdbd,)

.input
    position relative
    margin-top 8px
    margin-top $default-top-bottom-margin

    &:first-child
        margin-top 0

    label
        display block
        width auto

    &.text
        position relative

    label + input, label + textarea, label + select
        background red
        color red

.input.text input, .input.text textarea, .input select
    margin-top 16px
    margin-top 1rem
    width 100%
    height 32px
    height 2rem
    font-size 100%
    font-family inherit
    border 0
    border-bottom 1px solid #cfd0d1
    background-color inherit
    transition all .3s
    outline none
    border-radius 0 /* google chrome select */


    + label
        absolute left
        color #bdbdbd
        transition color .1s

    &:not([value]) + label, &[value=""] + label
        top 16px
        top 1rem
        height 32px
        height 2rem
        line-height 32px
        line-height 2rem
        font-size 100%
        color #bdbdbd
        cursor text
        transition all .3s

    &:focus, &:focus:invalid:not([value])
        border-bottom 1px solid $primary-color
        box-shadow 0 1px 0 0 $primary-color

    + label, &:focus + label, &[placeholder] + label
        top 1px
        height auto
        line-height inherit
        font-size 80%
        cursor default

    &:focus + label
        color $primary-color
        outline none

    &:invalid
        box-shadow none

    &:invalid[value]
        border-bottom 1px solid $invalid-color
        box-shadow 0 1px 0 0 $invalid-color

    &:focus:invalid[value] + label
        color $invalid-color

.input
    /* SELECT */

    select
        appearance none
        display inline-block
        &:focus
            outline none
            border-top 0
            border-left 0
            border-right 0
        &:-moz-focusring
            /* to avoid the ugly "outline" in firefox */
            transition none
            outline none
            color rgba(0,0,0,0)
            text-shadow 0 0 0 #000

    &.select:after
        content "▼"
        absolute top 1rem right bottom
        font-size 60%
        line-height 2rem
        height 2rem
        padding 0 .5rem
        pointer-events none

    /* RADIO AND CHECKBOX */

    /* Remove default radio and checkbox buttons */
    /* Everything but IE6-8 */
:root *>
    .input
        [type="radio"], [type="checkbox"]
            position absolute
            left -99999px
            visibility hidden
            & + label
                padding-left 1.5rem

.input
    [type="radio"], [type="checkbox"]
        & + label
            position relative
            cursor pointer
            display inline-block
            height 1rem
            line-height 1rem

            user-select none

            &::before, &::after /* supports IE >= 9 with :: */
                content ''
                position absolute
                left 0
                top 0
                margin .2rem
                width .6rem
                height .6rem
                z-index 0
                transition all .2s

    [type="checkbox"] & + label
        &::before, &::after
            transition: border .25s, background-color .25s, width .20s .1s, height .20s .1s, top .20s .1s, left .20s .1s;

    /* Unchecked styles */
    [type="radio"]:not(:checked) + label::before
        border-radius 50%
        border 2px solid #5a5a5a
    [type="radio"]:not(:checked) + label::after
        border-radius 50%
        border 2px solid #5a5a5a
        z-index -1

        transform scale(0)

    [type="checkbox"]:not(:checked) + label::before
        border 2px solid #5a5a5a

    /* Checked styles */
    [type="radio"]:checked
        & + label::before
            border-radius 50%
            border 2px solid transparent

        & + label::after
            border-radius 50%
            border 2px solid $primary-color
            background-color $primary-color

    [type="checkbox"]:checked
        & + label::before
            border-top 2px solid transparent
            border-left 2px solid transparent
            border-right 2px solid #fff
            border-bottom 2px solid #fff
            transform rotateZ(37deg)
            transform-origin 100% 100%
            z-index 1
            top 0
            left 1px
            width .2rem
            height .5rem

        & + label::after
            border 2px solid $primary-color
            background-color $primary-color


    /* DISABLED */

    input:disabled, textarea:disabled, option:disabled, optgroup:disabled, select:disabled
        background-color transparent
        color $disabled-color
        border-bottom 1px dotted $disabled-border-color

    [type="radio"]:disabled + label, [type="checkbox"]:disabled + label
        color $disabled-color

    [type="radio"]:disabled, [type="checkbox"]:disabled
        &:not(:checked) + label:before
            border-color $disabled-color


